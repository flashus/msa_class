spring:
  datasource:
    # url: jdbc:postgresql://localhost:5001
    url: jdbc:postgresql://localhost:5432/social
    username: pguser
    password: pgpass
    driver-class-name: org.postgresql.Driver

spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8008
